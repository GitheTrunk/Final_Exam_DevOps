---
- name: Deploy Spring Boot Application
  hosts: web
  become: false

  tasks:

    - name: Run tests
      shell: |
        cd /Users/macbookpro/.jenkins/workspace/football-terrain-rental
        mvn test

    - name: Build project
      shell: |
        cd /Users/macbookpro/.jenkins/workspace/football-terrain-rental
        mvn clean package -DskipTests

    - name: Backup MySQL database
      shell: |
        cd /Users/macbookpro/Documents/I4_ITC/S2/DevOps/football-terrain-rental
        /opt/homebrew/bin/mysqldump -h 127.0.0.1 -P 3307 \
        -u root -pHello@123 \
        bun_sengleang-db > backup.sql